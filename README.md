# Flutter Assets Manager

Manage Flutter assets easily with **assets_generator** and **as_icon_extension**.

## Files

* **assets_generator**: Generates `AssetsManager` class with all asset paths (PNG, SVG, Lottie).
* **as_icon_extension**: Simplifies using assets in widgets; auto-detects type and applies correct widget.

